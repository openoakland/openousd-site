version: '3.9'

services:
  gatsby:
    ports:
      - "8000:8000"
    build: .
    volumes:
      - .:/openousd
    stdin_open: true
    tty: true
    command: "gatsby develop -H 0.0.0.0 -p 8000"
    # entrypoint: /bin/sh
